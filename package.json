{
    "name": "simple-pages-project",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm run build:frontend && npm run build --prefix backend",
        "build:frontend": "npm run build --prefix frontend",
        "dev:frontend": "npm run dev --prefix frontend",
        "start:backend": "npm start --prefix backend"
    },
    "dependencies": {
        "bootstrap": "^5.3.8",
        "react": "^19.2.4",
        "react-dom": "^19.2.4"
    },
    "devDependencies": {
    }
}
